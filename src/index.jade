doctype html
html(
  lang="en"
  ng-app="app"
)
  head
    meta(charset="utf8")

    title #{title}
    meta(name="description", content="")

    link(rel="stylesheet", href="/css/app.css")

  body
    div(ui-view="main")

    each script in scripts
      script(src="#{script}")
